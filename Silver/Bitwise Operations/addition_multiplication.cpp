// INCOMPLETE

#include <bits/stdc++.h>
using namespace std;

// Addition of 2 integers using bit operations
int add(int a, int b) {
    while (b > 0) {
        ;
    }
    return ;
}

int main() {
    int a, b;
    cin >> a >> b;

    cout << add(a, b) << endl;
}
