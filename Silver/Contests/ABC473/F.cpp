#include <bits/stdc++.h>
using namespace std;

int main() {
    int N, Q; string S;
    cin >> N >> S >> Q;
    
    while (Q--) {
        int type; cin >> type;
        if (type==1) {  // replace letter
            int i; char c; cin >> i >> c; i--;
            ;
        } else {
            int l, r; cin >> l >> r; l--; r--;
            ;
        }
    }
}