#include <bits/stdc++.h>
using namespace std;
using ll = long long;

ll MOD = 998244353;

int main() {
    int t; cin >> t;
    while (t--) {
        int N; cin >> N;
        vector<int> a(N);
        for (auto& i : a) cin >> i;

        ;
    }
}