// USACO 2026 Second Contest, Silver
// Problem 1. Cow-libi 2

#include <bits/stdc++.h>
using namespace std;

int main() {
    int T, C;
    cin >> T >> C;
    while (T--) {
        int N; cin >> N;
        vector<int> left(N);
        for (int i{0}; i < N; ++i) {
            cin >> left[i];
        } vector<int> right(N);
        for (int i{0}; i < N; ++i) {
            cin >> right[i];
        }
    }
}
